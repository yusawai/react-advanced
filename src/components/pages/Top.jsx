import styled from "styled-components";

export const Top = () => {
  return (
    <SCContainer>
      <h2>This's home page.</h2>
    </SCContainer>
  );
};

const SCContainer = styled.div`
  text-align: center;
`;
